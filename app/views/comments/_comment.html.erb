<%= div_for comment do %>
  <p>
      <strong>
        <%= @username = User.find(session[:user_id]).name %> Posted <%= time_ago_in_words(comment.created_at) %> ago
      </strong>
        <br/>
        <%= comment.body %>
   </p>
<% end %>