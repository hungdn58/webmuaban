<li class="<%=  self_or_other(message) %>">
  <div class="avatar">
    <%= gravatar_for message_interlocutor(message) %>
  </div>
  <div class="chatboxmessagecontent">
    <p><%= message.body %></p>
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.localtime.strftime("%-m/%-d/%Y at %-I:%M %p") %>">
      <%= message_interlocutor(message).name %> • <%= message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>